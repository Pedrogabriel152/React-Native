import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    category: {
        width: '100%',
        flexDirection: 'row',
        justifyContent: 'space-evenly',
        marginTop: 30,
        marginBottom: 15,
    },

    categoryText:{
        fontSize: 14,
        fontWeight: '700',
        color: '#fff'
    }
})